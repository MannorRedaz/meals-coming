sequenceDiagram
    participant User as 用户
    participant Frontend as 前端
    participant Backend as 后端
    participant Database as MySQL数据库

    User->>Frontend: 点击已完成订单进行评价
    Frontend->>Backend: 携带订单号发起 AXIOS GET 请求
    Backend->>Backend: EvaluationController 处理请求
    Backend->>Backend: 调用 getByOrderId()
    Backend->>Database: 发起数据库查询请求
    alt 查询到数据
        Database-->>Backend: 返回查询数据
        Backend->>Frontend: 返回封装好的数据
        Frontend->>Frontend: 渲染评价内容
    else 未查询到数据
        Backend->>Frontend: 返回空数据
        Frontend->>Frontend: 不展示数据
    end

    User->>Frontend: 填写评价内容（菜品、套餐、商家、服务等维度）
    User->>Frontend: 选择 1 - 5 级星级评分
    User->>Frontend: 提交评价
    Frontend->>Frontend: 校验评价内容和评分是否为空
    alt 校验通过
        Frontend->>Backend: 调用 saveEvaluationApi 发起 POST 请求
        Backend->>Backend: EvaluationController 接收请求
        Backend->>Backend: 调用 save() 方法
        Backend->>Database: 保存评价数据
        Database-->>Backend: 返回保存结果
        Backend->>Frontend: 返回封装保存结果信息
    else 校验不通过
        Frontend->>Frontend: 提示为空的信息
    end